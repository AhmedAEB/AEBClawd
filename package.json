{
  "name": "aebclawd",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter frontend dev & pnpm --filter server dev",
    "frontend:dev": "pnpm --filter frontend dev",
    "server:dev": "pnpm --filter server dev"
  }
}
